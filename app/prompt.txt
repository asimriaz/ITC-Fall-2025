FIRST PROMPT:
I want to create a web application to manage a list of countries. 
The app should display a table of 20 countries with ID, name, and status. 
Users should be able to search countries by name . 
The table should allow inline editing: clicking on a row should let users edit the country name and change its status between "Visited" and "Not Visited". 
All changes must be saved to a JSON file using a Flask backend. 
Use HTML, JavaScript, and Tailwind CSS for a clean, responsive design. 
Provide the full Flask backend code, the JSON file with 20 countries, and the HTML template.
#SECOND PROMPT: 
The table must have proper borders and styling, and users should be able to edit country names and statuses inline directly in the table. 
Include a search feature that filters countries by name in real time. 
The app should use Python Flask for the backend, JSON to store data, HTML/JS for the frontend, and Tailwind CSS for modern styling. 
#THIED PROMPT:
Build a web app to manage 20 countries with ID, name, and status. 
Features:
- Inline editing of name and status directly in the table
- Search functionality by country name
- Status options: Visited / Not Visited
- Save changes to a JSON file through Flask API
Use HTML, JavaScript, and Tailwind CSS for frontend and styling. 
Provide full Flask backend code and HTML template.

