# Country Management Web App

This is a **web application to manage a list of countries**. It allows users to **view, search, and edit** the name and status of countries using a modern inline editable table with Tailwind CSS styling.


## Features

- Display a list of **20 countries** with ID, name, and status.
- **Inline editing**: click on a row to edit the country name and status directly in the table.
- **Search countries** by name (works with 3+ letters).
- **Status options**: "Visited" or "Not Visited".
- **Modern, responsive styling** with Tailwind CSS.
- Saves changes **directly to the backend** (via Flask API endpoints).


## Technologies Used

- **Python** (Flask framework)  
- **HTML / CSS / JavaScript**  
- **Tailwind CSS** for styling  
- **JSON** file to store country data  

## Project Structure

your-project/
│
├─ app.py               # Flask backend code
├─ countries.json       # The JSON file storing country data
├─ templates/
│   └─ index.html       # HTML page for the web app
├─ README.md            #  project + prompt info
## How to Run

1. Make sure Flask is installed:

```bash
pip install flask

---


 all names of people who worked on it:


- Sheher Bano
- hifza batool
- ansoya 
